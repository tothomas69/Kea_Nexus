# Pinned, not floated. A >= range let the container rebuild onto a Streamlit
# whose tabs use React Aria while the local venv had one using BaseWeb —
# different DOM, different CSS hooks — so styling verified locally did not
# match what shipped. Bump deliberately, and re-check style.css's widget
# selectors when you do.
streamlit==1.62.0
httpx>=0.27.0
pandas>=2.2.0
python-dotenv>=1.0.1
extra-streamlit-components>=0.1.60
